import { CustomerSubscription } from "../domain/customer-subscription";

export class SubscriptionService {
  find(customerId: string): CustomerSubscription | null {
    void customerId;
    return null;
  }
}
