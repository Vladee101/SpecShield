{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum surface for Workflow A. Deliberately grants no network, no shell, and no arbitrary filesystem access: SpecShield claims to work offline (PRD \u00a710), and a capability that could reach the network would make that claim untestable. Clipboard is write-only \u2014 reading the user's clipboard is not this app's business.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "clipboard-manager:allow-write-text"
  ]
}
